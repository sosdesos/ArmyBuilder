@model IEnumerable<BusinessLogicLayer.UserBLL>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-hover">
    <tr>
        <th>
            @*@Html.DisplayNameFor(model => model.UserID)*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Hash)*@
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.Salt)*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmailAddress)
        </th>
        <th>
            @*@Html.DisplayNameFor(model => model.RoleID)*@
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rolename)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.HiddenFor(modelItem => item.UserID)
        </td>
        <td>
          @*@Html.DisplayFor(modelItem => item.UserName)*@
          @Html.ActionLink(item.UserName, "Details", new { id = item.UserID }, null)

        </td>
        <td>  
            @Html.HiddenFor(modelItem => item.Hash)
        </td>
        <td>
            @Html.HiddenFor(modelItem => item.Salt)
        </td>
        <td>
          @Html.DisplayFor(modelItem => item.EmailAddress)
        </td>
        <td>
            @Html.HiddenFor(modelItem => item.RoleID)
        </td>
        <td>
          @*@Html.DisplayFor(modelItem => item.Rolename)*@
          @Html.ActionLink(item.Rolename, "Details", "Roles", new { id = item.RoleID }, null)

        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserID })
        </td>
    </tr>
}

</table>
